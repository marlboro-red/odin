Implement the fix described in PLAN for issue #{{ params.issue_number }}.

Constraints:
- Make the smallest change that fixes the flake.
- Do not weaken or delete assertions to make the test pass.
- Keep the change limited to the files named in the plan.

When you are done, ensure `cargo test --workspace` passes locally.
